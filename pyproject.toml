[project]
name = "vpn-proxy"
version = "0.1.0"
description = "Oversecure Subscription Proxy with Happ, Saywallahi and Mihomo/Clash support"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.1",
    "fastapi>=0.139.0",
    "uvicorn>=0.51.0",
    "pyyaml>=6.0.1",
]
